class SpottRadius {
  static const double xs = 6.0;
  static const double sm = 8.0;
  static const double md = 18.0; // Button radius (18.0)
  static const double lg = 20.0; // Secondary card radius (20.0)
  static const double xl = 24.0; // Primary card radius (24.0)
  static const double xxl = 28.0;
  
  static const double primaryCard = 24.0;
  static const double secondaryCard = 20.0;
  static const double button = 18.0;
  static const double card = 24.0; // Primary card mapped by default
  static const double pill = 999.0;
  static const double circular = 1000.0;

  // ── Scene-specific radii ────────────────────────────────────────────
  /// Hero search scene — softer than everything else
  static const double hero = 30.0;
  /// Promotional banners — premium curved feel
  static const double banner = 28.0;
  /// Bottom navigation bar
  static const double nav = 30.0;
}
