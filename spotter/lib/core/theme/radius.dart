import '../../design_system/tokens/radius.dart';

class SpottRadius {
  static const double xs = DSRadius.xs;
  static const double sm = DSRadius.sm;
  static const double md = DSRadius.md; // List Items / Sub-cards
  static const double lg = DSRadius.lg; // Input / Button
  static const double xl = DSRadius.xl; // Primary Cards / Banners
  static const double xxl = DSRadius.xxl; // Bottom Sheets / Search Containers
  
  static const double primaryCard = DSRadius.card;
  static const double secondaryCard = DSRadius.card;
  static const double button = DSRadius.button;
  static const double card = DSRadius.card; 
  static const double inputs = DSRadius.input;
  static const double bottomSheet = DSRadius.bottomSheet;
  static const double searchContainer = DSRadius.bottomSheet;
  static const double pill = DSRadius.pill;
  static const double circular = DSRadius.pill;

  // ── Scene-specific radii ────────────────────────────────────────────
  static const double hero = DSRadius.card;
  static const double banner = DSRadius.card;
  static const double nav = DSRadius.lg;
  static const double floatingSurface = DSRadius.bottomSheet;
}
